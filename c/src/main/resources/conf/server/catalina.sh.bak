##JDK 1.8.u111
/etc/profile
export JAVA_HOME=/usr/java/jdk1.8.0_111
export CLASSPATH=.:$JAVA_HOME/jre/lib/rt.jar:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar
export PATH=$PATH:$JAVA_HOME/bin:${ANT_HOME}/bin:$NODE_HOME/bin

test:java -version


##tomcat
catalina.sh
if [[ "$JAVA_OPTS" != *-Djava.security.egd=* ]]; then
    JAVA_OPTS="$JAVA_OPTS -Djava.security.egd=file:/dev/./urandom"
fi

server.xml
<Context path="/jt-c" docBase="/opt/project/jt-c" reloadable="true" debug="0" crossContext="true"/>
<Context path="/jt-io" docBase="/opt/project/jt-io" reloadable="true" debug="0" crossContext="true"/>
</HOST>

##redis
redis.io stable
wget redis-3.2.6.tar.gz
tar xzvf redis-3.2.6.tar.gz
cd redis-3.2.6
make  (yum install gcc)

redis.conf 10010  daemonize no->yes
src/redis-server redis.conf
src/redis-cli -p 10010
test -> set key get key

zookeeper
zookeeper.apache.org wget 3.4.8.tar.gz
conf/zoo.cfg  -> create dataDir=/opt/data/zookeeper
bin/zkServer.sh start
bin/ZkServer.sh stop/restart

##Mariadb
/etc/my.cnf
/etc/my.cnf.d/server.cnf
systemctl maridb.service restart/start/stop

/opt/projects
/opt/projects/jt-p <=jt-p.zip   unzip(yum install unzip)
/opt/projects/jt-c <=jt-c.1.0.war  jar xzvf jt-c.1.0.war => mv jt-c.1.0 jt-c
/opt/projects/jt-io










